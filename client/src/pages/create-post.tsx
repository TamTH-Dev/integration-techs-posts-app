// import { useCheckAuth } from "../utils/useCheckAuth"

const CreatePost = () => {
  // const {data, loading} = useCheckAuth()

  return (
    <div>HEllo world</div>
  )
}

export default CreatePost
